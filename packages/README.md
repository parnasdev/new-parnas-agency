create your Package in root of packages folder

**add your package config in a register.json**

example:

```json
{
    "packageName": "seo",
    "service" : "path of service provider",
    "disabled": false
}

```
